/* ============================================================
   MAIN — Entry point, initializes all modules
   ============================================================ */

(function() {
  'use strict';

  // Kick off each module (all self-invoking, order doesn't matter)
  // Modules: typing.js, parallax.js, scrollReveal.js, countUp.js, timelineGlow.js
  // Each is loaded as a separate <script> in index.html

})();
